%h1 Listing capsuls
- if current_user
  = link_to "#{t 'index.new_capsul'}", new_capsul_path
%br

%table
  %tr
    %th=t 'capsuls.description'
    %th=t 'capsuls.video'
    %th=t 'capsuls.user'
    %th
    %th
    %th

  - @capsuls.each do |capsul|
    %tr
      %td= capsul.description
      %td= capsul.video
      - owner = User.find(capsul.user_id) if capsul
      %td= link_to owner.username, user_path(owner)
      %td= link_to "#{t 'capsuls_link.show'}", capsul_path(capsul)
